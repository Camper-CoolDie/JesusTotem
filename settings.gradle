pluginManagement {
    repositories {
        gradlePluginPortal()
        // NeoForge
        maven { url = 'https://maven.neoforged.net/releases/' }
    }
}

rootProject.name = 'JesusTotem'
include('NeoForge')
